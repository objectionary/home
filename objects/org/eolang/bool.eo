+package org.eolang
+java org.eolang:eo-runtime:0.1.10

[data] > bool
  # If true, return "t", otherwise "f"
  [t f] > if /t

  # NOT
  [] > not /bool

  # AND
  [x...] > and /bool

  # OR
  [x...] > or /bool

  # While this is true copies the object
  # with "i" as the position of the cycle. The result
  # of WHILE is the number of successful cycles.
  [f] > while /int
